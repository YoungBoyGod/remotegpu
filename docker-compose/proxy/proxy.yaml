# RemoteGPU Proxy 配置文件
port: 9090

server:
  url: "http://localhost:8080"
  proxy_id: "proxy-01"
  token: ""
  timeout: 30s

port_pool:
  range_start: 20000
  range_end: 60000

heartbeat:
  interval: 30s

http_proxy:
  enabled: true
  port: 9091

network:
  inner_ip: ""
  outer_ip: ""
